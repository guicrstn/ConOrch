apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: workshop
  labels:
    app: postgres
spec:
  clusterIP: None  # headless
  selector:
    app: postgres
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
